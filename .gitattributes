# Keep text normalization only. Do not use Git LFS for site assets —
# GreenGeeks / cPanel git clone does not fetch LFS objects, so images
# would deploy as tiny pointer files and break in the browser.
* text=auto
